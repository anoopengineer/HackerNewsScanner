<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="HNTheme">
        <!-- Customize your theme here. -->
    </style>

    <style name="HNTheme" parent="@android:style/Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
    </style>

    <style name="MyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/primary</item>
    </style>

    <style name="comments_author">
        <item name="android:textColor">@color/commentTextColor</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/hn_comments_font_size</item>
    </style>
    <style name="comments_date">
        <item name="android:textColor">@color/commentTextColor</item>
        <item name="android:textSize">@dimen/hn_comments_font_size</item>
    </style>
    <style name="comments_text">
        <item name="android:textColor">@color/commentTextColor</item>
        <item name="android:textSize">@dimen/hn_comments_font_size</item>
    </style>

</resources>
